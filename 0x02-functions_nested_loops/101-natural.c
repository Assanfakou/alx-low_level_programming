#include <stdio.h>
/* int add(int a)
{
	if(a > 0)
		return a +;
}*/

int main ()
{
	int a, total, mul;
	
	a = 0;
	while(a < 1024)
	{
		if(a % 3 == 0 || a % 5 == 0)
		{
			a += total;
			printf("%d ", total);
		}
		a++;
	}
	printf("\n");
	return (0);
}

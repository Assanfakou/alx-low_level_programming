#include "main.h"
/**
 * int _abs - return a negative probleme to positive
 * @l: integer
 * Return: Number of digits in l
 */
int _abs(int l)
{
	if (l < 0)
	{
		return (l * -1);
	}
	else
	return (l);
}
